# fixtures/ — things pytest runs around every test automatically.
#
# These modules are loaded by conftest.py (see its pytest_plugins line).
# They contain pytest FIXTURES (setup/teardown that wraps each test) and
# HOOKS (functions pytest calls at specific moments).
