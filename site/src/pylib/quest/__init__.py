"""The quest site's own Python: the code-review rubric and the run harness.

Kept separate from playwright_lite so the shim stays a pure Playwright
surface with nothing quest-specific leaking into it.
"""
